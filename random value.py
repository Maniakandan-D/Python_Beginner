# Find Random Value
import random

for i in range(3):  # how many value you want to enter the input
    print("The Random value is :", random.randint(10, 20))
    # Between two random numbers to enter the parenthesis
